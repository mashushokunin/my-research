# Simulink Models

通信条件を動的に模擬する必要がある実験で、Simulinkモデルを管理します。

## Planned scope

- 通信帯域の上限
- 送信周期とキーフレーム頻度
- 通信遅延
- パケットキューとドロップ
- 共有方式ごとの送信データ量

最初の必須実験はMATLABによるオフライン評価で実装します。`bandwidth_model.slx`などのモデルは、相対姿勢推定の実験系が完成してから追加します。
